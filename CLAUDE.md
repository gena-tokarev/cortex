# Workspace Notes

- The active workspace uses Bun workspaces and Turborepo.
- Prefer root scripts or `turbo run ... --filter=...` for app and package tasks.
- Active apps live under `apps/`.
- Shared workspace packages live under `libs/`.
